# AGENTS.md - benchmarks

## Scope
- 本目录用于 kctsb 与 OpenSSL 的性能对比基准测试。
- 仅展示可复现、可比较的测量方法与参数。

## 约束
- RSA 基准仅允许 3072/4096 位，且仅 OAEP(PKCS#1 v2.2) 与 PSS(SHA-256)。
- 禁止引入 RSA-PKCS1-v1_5 或 Raw RSA。
- 不引入额外依赖库，保持与主库一致的编译选项。

## 设计原则
- 基准需明确标注算法、参数、实现与结果含义。
- 任何新增基准必须对齐固定输入与迭代策略。
