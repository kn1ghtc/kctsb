# AGENTS.md - src/crypto

## Scope
- 单文件单算法实现与 C ABI 导出。

## 约束
- 禁止新增外部加密依赖。
- RSA 仅支持 3072/4096 位，且只保留 OAEP 与 PSS（SHA-256）。
- 哈希必须使用 kctsb 内置 SHA-256（已启用硬件加速），避免重复实现。

## 安全要求
- 关键路径尽量采用固定窗口/固定步数流程，降低侧信道风险。
- 仅使用恒定时间比较函数处理敏感数据。
